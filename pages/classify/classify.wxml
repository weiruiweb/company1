<!--pages/ classify.wxml-->
<block wx:for="{{web_mainData}}" wx:key="id">
	<view class="submit_info" bindtap="tabPath" style="margin-top:20rpx" data-path="{{item.url}}">
		{{item.title}}
	</view>
</block>

